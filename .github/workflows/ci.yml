name: ci

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: pnpm
        uses: pnpm/action-setup@d9184bf108216479bc5a137cc391f4d7b14c870b # v6.1.0
        with:
          run_install: false

      - name: node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: install
        run: pnpm install --frozen-lockfile

      # svelte-package output must exist first: site/panda.config.ts and
      # svelte-check both resolve sigil-ui/* exports through dist/.
      - name: package
        run: pnpm build

      # styled-system/ is gitignored; generate it so site type imports resolve.
      - name: panda codegen
        run: pnpm -F sigil-site codegen

      - name: typecheck
        run: pnpm check

      - name: lint and format
        run: pnpm lint && pnpm format:check

      - name: test with coverage gates
        run: pnpm test:coverage

      - name: lint artifact
        run: pnpm pack:check

      - name: build site
        run: pnpm -F sigil-site build
