{
  "name": "sigil-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "sigil-ui css && node ../scripts/gen-sizes.mjs",
    "predev": "pnpm -C .. build && sigil-ui css && node ../scripts/gen-sizes.mjs",
    "dev": "vite",
    "prebuild": "pnpm -C .. build && node ../scripts/gen-llms.mjs && sigil-ui css && node ../scripts/gen-sizes.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sigil-ui": "workspace:*"
  },
  "devDependencies": {
    "@lucide/svelte": "1.42.0",
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte": "5.57.0",
    "vite": "8.2.2"
  }
}
