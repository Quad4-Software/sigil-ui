{
  "name": "sigil-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "codegen": "panda",
    "predev": "pnpm -C .. build && panda",
    "dev": "vite",
    "prebuild": "pnpm -C .. build && node ../scripts/gen-llms.mjs && panda",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sigil-ui": "workspace:*"
  },
  "devDependencies": {
    "@lucide/svelte": "1.42.0",
    "@pandacss/dev": "1.12.1",
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte": "5.57.0",
    "vite": "8.2.2"
  }
}
