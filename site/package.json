{
  "name": "sigil-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm -C .. build",
    "dev": "vite",
    "prebuild": "pnpm -C .. build && node ../scripts/gen-llms.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sigil-ui": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "svelte": "5.57.0",
    "unocss": "66.10.0",
    "vite": "8.2.2"
  }
}
